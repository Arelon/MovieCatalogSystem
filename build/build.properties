root.dir=.
src.dir=${root.dir}/source/java

startup.dir=${root.dir}/startup
export.dir=${startup.dir}/export
restore.dir=${startup.dir}/restore

build.dir=${root.dir}/build
compile.dir=${build.dir}/compile
output.dir=${build.dir}/output

explicit.lib.dir=${root.dir}/lib

build.overrides.dir=${root.dir}/build-overrides
build.overrides.installation.dir=${build.overrides.dir}/installation
build.overrides.source.dir=${build.overrides.dir}/source

build.properties.file=${src.dir}/net/milanaleksic/mcs/version.properties

src.metamodel.dir=${root.dir}/source/java-generated-metamodel
src.metamodel.includes=/net/milanaleksic/mcs/domain/model/*.java

ivy.install.version=2.2.0
ivy.jar.dir=${build.dir}/ivy
ivy.jar.file=${ivy.jar.dir}/ivy.jar
ivy.home=${user.home}/.ant
ivy.resolved.libs.target.dir=${build.dir}/lib

src.mainClass=net.milanaleksic.mcs.Startup

nsis.dont.delete.installation.files=false
nsis.dont.create.installation=false
nsis.home=c:/Program Files/NSIS
nsis.setup.script=${build.dir}/setupScript.nsi
install.dir=${build.dir}/install